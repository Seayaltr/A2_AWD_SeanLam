# Assignment 2 Airline Booking Starter

1. Run `npm install`
2. Copy `.env.local.example` to `.env.local`
3. Add your MongoDB Atlas connection string
4. Run `npm run seed`
5. Run `npm run dev`
6. Open http://localhost:3000
